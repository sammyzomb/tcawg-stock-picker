# 圖庫下載說明

> **給 AI 用**：使用者說「依照圖庫下載說明」時，讀完本檔後**直接執行**，不要再問套件或參考專案路徑。

## 固定路徑（勿改）

| 用途 | 路徑 |
|------|------|
| 共用下載引擎 | `D:/GITHUB_2/圖庫下載說明` |
| 薄包裝參考專案 | `D:/GITHUB_2/UIO16A 加拉巴哥群島 厄瓜多 哥倫比亞雙城 咖啡風情 16日` |
| 本專案根目錄 | 目前 Cursor workspace 根目錄 |

## 第一步：判斷狀態

在**本專案根目錄**檢查是否已接入：

- 已有 `stock-picker.config.json`
- 已有 `scripts/image-slots.json`
- 已有 `scripts/find-images.js`（或 `scripts/package.json` 含 `tcawg-stock-picker`）

**缺任一項 → 執行「首次接入」**  
**齊全 → 執行「日常作業」**（依使用者這輪對話的具體需求）

---

## 首次接入（新專案，一次做完）

1. 從參考專案複製到本專案 `scripts/`：
   - `find-images.js`、`fill-slots.js`、`sync-slot-images.js`
   - `setup-env.ps1`、`lib/stock-picker-path.cjs`（參考專案 `scripts/lib/` 整包複製到本專案 `scripts/lib/`）
   - `sstk-search.js`、`sstk-license.js`（可選）
2. 建立 `scripts/package.json`（**勿**複製 UIO16A 整包；該專案另有 `sharp` 等依賴）：
   ```json
   { "private": true, "dependencies": { "tcawg-stock-picker": "file:../../圖庫下載說明" } }
   ```
3. 複製 `D:/GITHUB_2/圖庫下載說明/templates/stock-picker.config.example.json` → 本專案根 `stock-picker.config.json`；`licenseMarkdown` / `creditsManifest` 可改成本專案 `images/【代號】-shutter/...`（**非必填**，`targetRoot` 正確時程式會自動推導路徑）
4. 複製 `D:/GITHUB_2/圖庫下載說明/templates/image-slots.example.json` → `scripts/image-slots.json`，改 `project`、`targetRoot`、`slots`（**勿**複製 UIO16A 的 slots 內容）
5. 建立 archive 資料夾，各放空 `credits.json`（格式 `[]`）：
   - 圖片：`public/images/{pexels,unsplash,shutterstock}/credits.json`
   - 影片（若專案需要）：`public/videos/{pexels,shutterstock}/credits.json`
6. 確認共用金鑰檔已存在：`D:/GITHUB_2/圖庫下載說明/.preview/stock-api.env`（首次從 `templates/stock-api.env.example` 複製並填入 Pexels / Unsplash / Shutterstock；**勿 commit**）
7. 執行：
   ```powershell
   cd scripts; npm install; cd ..
   powershell -File scripts/setup-env.ps1
   node scripts/find-images.js --check-keys
   ```
8. 複製 `D:/GITHUB_2/圖庫下載說明/templates/圖庫下載.cursor-rule.mdc` → 本專案 `.cursor/rules/圖庫下載.mdc`（若尚未存在）

接入完成後簡報：專案代號、`targetRoot`、check-keys 結果。

---

## 日常作業

在**本專案根目錄**執行（先 `cd` 到根目錄）：

| 使用者意圖 | 指令 |
|------------|------|
| 檢查金鑰 | `node scripts/find-images.js --check-keys` |
| 查剩餘額度 | `node scripts/find-images.js --check-quota` |
| 列出所有 slot | `node scripts/find-images.js --list-slots` |
| 手動挑圖 | `node scripts/find-images.js --slot <id>` 或 `"關鍵字"` |
| 手動挑影片 | `node scripts/find-images.js --video "關鍵字"` 或 `--slot <video-id>` |
| 批次補圖（免費） | `node scripts/fill-slots.js --all --free-only` |
| 批次補圖（含 SS，扣張數） | `node scripts/fill-slots.js --all --confirm-license` |
| 只換小圖/糊圖 | `node scripts/fill-slots.js --weak --free-only` |
| 單一 slot | `node scripts/fill-slots.js --slot <id>` |
| 同步檢查 | `node scripts/sync-slot-images.js --verify-only` |
| 重建授權說明 | `node scripts/fill-slots.js --rebuild-only` |

**互動搜尋**：先列出預覽 → 等使用者選編號 → 才下載（不可跳過）。

**關鍵字設定**（`scripts/image-slots.json` 每個 slot）：
- `query`：主要英文關鍵字（必填）
- `altQueries`：備用關鍵字陣列（搜不到時依序重試）
- `type`：`"image"`（預設）或 `"video"`；也可由 `.mp4` 副檔名自動判斷

**搜尋優先**：
- 圖片：Shutterstock → Unsplash → Pexels（`providerPriority`）
- 影片：Shutterstock → Pexels（`videoProviderPriority`；Unsplash 無影片 API）

**檔案位置**：
- 圖片 archive：`public/images/{shutterstock,unsplash,pexels}/` + `credits.json`
- 影片 archive：`public/videos/{shutterstock,pexels}/` + `credits.json`
- HTML 素材：`images/<專案>-shutter/`（見 `targetRoot`）
- 授權：`圖片授權說明.md`

**未建薄包裝時**：可直接呼叫 `D:/GITHUB_2/圖庫下載說明/bin/stock-*.js`（加 `--project` 指定專案根目錄），見 `README.md`。

---

## 硬性規則

- 程式只改 `D:/GITHUB_2/圖庫下載說明`（npm 套件名 `tcawg-stock-picker`）；行程專案只改設定、`image-slots`、素材路徑
- **永不**把 API Key 寫進 HTML/JS；`.env` 勿 commit
- Shutterstock 授權會扣訂閱張數；批次前若未明示，用 `--free-only` 或先提醒
- 不要複製參考專案的 `image-slots.json` 或 `images/UIO16A-shutter/` 圖檔到新專案

---

## 新專案只需帶這一樣

複製 **`D:/GITHUB_2/圖庫下載說明/templates/圖庫下載.cursor-rule.mdc`** 到新專案 **`.cursor/rules/圖庫下載.mdc`**。  
之後在該專案對 AI 說：**「依照圖庫下載說明」** 即可。

（進階可選：`templates/cursor-rule-stock-picker.mdc.example` → `.cursor/rules/stock-image-search.mdc`，開啟圖槽相關檔案時自動提示；與上面的觸發規則可並存。）
